# Copy this to .env and fill in your values before deploying

# PostgreSQL
PG_DB=har_router
PG_USER=postgres
PG_PASSWORD=changeme_use_a_strong_password

# Optional — only needed if you want to override the /api proxy
# VITE_API_URL=https://your-ec2-domain.com
